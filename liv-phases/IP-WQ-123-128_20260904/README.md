# IP-WQ-123–128 packet

Boot this folder when a conversation says **agentify** and has no Short in the message.

1. Read `STATUS_BOARD.md`.
2. Run boot order in `IP-WQ-123_agentify_boot.md`.
3. Load `registry/characters.json`.
4. Offer `menus/PLATE_BACKLOG.md` or `menus/SERIALIZE_BACKLOG.md` — never both as one list.
5. Open `menus/shorts/<youtube-id>.md` for the active reel.
