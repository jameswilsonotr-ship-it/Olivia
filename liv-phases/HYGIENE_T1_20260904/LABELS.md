# Allowed labels (four live + three leftover)

LIVE — CLI smoked and used in a hop.
LIVE-DRAFT — code/files on disk; cold chat has not called it alone.
DONE — original done-when hit. Do not reopen for extras.
QUEUED — scheduled, not started, or waiting on a named pick.

Leftover stamps (not a fifth live state):
STUB — interface exists; fetch/body missing.
BLOCKED — missing tool, cookie, or client.
FANTASY — we refused the work on purpose (live scrapers).
